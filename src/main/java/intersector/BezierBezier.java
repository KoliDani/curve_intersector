package intersector;

import data.linetypes.Bezier;

public class BezierBezier extends Intersect {
    public BezierBezier(IntersectParser intersectParser) {
        Bezier l1 = (Bezier) intersectParser.l1;
        Bezier l2 = (Bezier) intersectParser.l2;

        System.out.printf("Not implemented yet");
    }
}
